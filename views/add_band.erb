<h1 class="featurette-heading">Add a new <span class="text-muted">Band</span></h1>
<hr class="featurette-divider">
<div class="form">
  <form action='/bands' method= "post">
    <label for="name"><p class="lead">Band Name:</p></label>
    <input id="name" name="name" type="text">
    <br>
    <div id="check_group">
      <% if @venues.any?() %>
        <br><br>
        <label for="checkbox"><p class="lead">What <span class="text-muted">venue(s)</span> has this band played at?</p></label><br><br>
        <% @venues.each() do |venue| %>
          <input type="checkbox" name="venue[]" value="<%= venue.id() %>"><strong><%= venue.name() %></strong></input><br><br>
        <% end %>
      <% else %>
          <p><em>There are currently no venues in the database.</em></p>
      <% end %>
      <p class="lead">(You can add more venues <a href="/add_venue">here</a>!)</p></em>
    </div>
    <br>
    <button class="btn btn-md btn-primary" type="submit">Add Band</button>
  </form>
</div>

<h1 class="featurette-heading">Edit band <span class="text-muted"><%= @band.name() %></span></h1>
<hr class="featurette-divider">
<div class="form">
  <form action='/bands/<%= @band.id() %>' method= "post">
  <input name="_method" type="hidden" value="patch">
  <label for="name"><p class="lead">Band Name:</label>
  <input id="name" name="name" type="text">
  <label for="checkbox"><p class="lead">Add any additional venues this band has played at:</p></label><br>
    <% @venues.each() do |venue| %>
      <input type="checkbox" name="venue[]" value="<%= venue.id() %>"><%= venue.name() %></a></input><br><br>
    <% end %>
  <button type="submit">Update Band</button>

  </form>
</div>
